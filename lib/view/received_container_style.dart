import 'package:flutter/material.dart';

// Text Style
const textStyle = TextStyle(fontWeight: FontWeight.bold);
const btnTextStyle = TextStyle(color: Colors.white);

// Edge Insets
const paddingStyle = EdgeInsets.symmetric(vertical: 20.0, horizontal: 10.0);
const containerMargin = EdgeInsets.symmetric(vertical: 10.0);
const containerPadding = EdgeInsets.all(8.0);
const btnPadding = EdgeInsets.symmetric(vertical: 5.0, horizontal: 10.0);

// Decorations
var containerDecoration = BoxDecoration(
    borderRadius: BorderRadius.circular(10.0),
    color: const Color(0xF0F0F0AB));

var btnDecoration = BoxDecoration(
    color: Colors.black,
    borderRadius: BorderRadius.circular(10.0));



